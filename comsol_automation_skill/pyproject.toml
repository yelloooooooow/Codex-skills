[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "comsol-automation-skill"
version = "0.1.0"
description = "Reusable COMSOL Server and Python/mph automation skill resources."
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Anonymous"}]

[tool.setuptools.packages.find]
include = ["comsol_skill*"]
