project_name: example_coupled
workflow_mode: coupled
software_paths:
  matlab_exe: "<path-to-matlab>"
  python_exe: "<path-to-python>"
  fluent_exe: "<path-to-fluent>"
outputs:
  root: outputs
cases:
  - name: baseline
parameters:
  unit_system: SI
report_options:
  generate_final_audit: true
