# Examples

## Simulink-only

Use `assets/examples/example_simulink_only_project.yaml` and the MATLAB scripts under `scripts/matlab/`.

## Fluent-only

Use `assets/examples/example_fluent_only_project.yaml` and scripts under `scripts/python/`.

## Coupled

Use `assets/examples/example_coupled_project.yaml`, export Simulink CSVs, compare with Fluent CSVs, and save calibrated results separately.
