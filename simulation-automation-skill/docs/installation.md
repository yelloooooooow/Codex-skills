# Installation

Copy `simulation-automation-skill/` into your Agent skills directory, for example:

```text
<CODEX_HOME>/skills/simulation-automation-skill
```

Reload the Agent environment. MATLAB/Simulink, Ansys Fluent, and PyFluent are not bundled and must be installed and licensed by the user.
